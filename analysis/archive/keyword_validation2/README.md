# Deprecated — moved to keyword_pipeline/

This folder previously contained the manual keyword validation workflow. It has been archived to `analysis/archive/keyword_validation/` for historical reference.

All active keyword work now uses the pipeline in `analysis/keyword_pipeline/`.
