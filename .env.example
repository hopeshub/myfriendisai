# Reddit OAuth credentials (optional — only needed if/when API access is approved)
# Without these, the collector uses public .json endpoints (no credentials needed)
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USERNAME=
REDDIT_PASSWORD=

# Redlib instance URL (for NSFW subreddit access — Step 6b)
REDLIB_URL=http://localhost:8080
